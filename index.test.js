/*import { render, screen, waitForElementToBeRemoved } from '@testing-library/react';
import { Inicial } from '.';

test('deve renderizar o post e retirar a mensagem de Carregando', async() => {
  render(<Inicial />);
  const carregando = screen.getByText('Carregando...');
  await waitForElementToBeRemoved(carregando);
  screen.debug();
});
*/