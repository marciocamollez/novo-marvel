/*import { render, screen } from '@testing-library/react';
import { Tabela } from '../../pages/Inicial/index';

describe("Componente de Tabela", () => {
    test('deve encontrar o texto da div que é renderizada em caso de sucesso', () => {
        render(<Tabela />);
        const geraTabela = screen.getByTestId("personagem-renderizado");
        expect(geraTabela);
    });
      
});
*/